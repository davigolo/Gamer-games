<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".views.login.MainLoginWindow.MainLoginFragment">

    <ImageView
        android:id="@+id/iv_main_login_window"
        android:layout_width="match_parent"
        android:layout_height="300dp"
        android:layout_marginHorizontal="20dp"
        android:layout_marginTop="13dp"
        android:contentDescription="@string/app_logo"
        android:scaleType="fitCenter"
        android:src="@drawable/logo_app"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/btn_main_login_window"
        style="@style/LoginButtonStyle"
        android:layout_width="match_parent"
        android:layout_height="55dp"
        android:layout_marginHorizontal="10dp"
        android:layout_marginTop="30dp"
        android:text="@string/log_in"
        android:textSize="20sp"
        app:layout_constraintTop_toBottomOf="@id/iv_main_login_window" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_main_signin_window"
        android:layout_width="match_parent"
        android:layout_height="55dp"
        style="@style/SignInButtonStyle"
        android:layout_margin="10dp"
        android:text="@string/sign_in"
        app:layout_constraintTop_toBottomOf="@id/btn_main_login_window" />

    <TextView
        android:id="@+id/tv_main_login_window"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:text="@string/play_it_save_it_share_it"
        android:textAlignment="center"
        app:layout_constraintTop_toBottomOf="@id/btn_main_signin_window" />

</androidx.constraintlayout.widget.ConstraintLayout>